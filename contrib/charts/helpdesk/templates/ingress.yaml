{{- if .Values.ingress.enabled -}}
{{- $fullName := include "helpdesk.fullname" . -}}
{{- $ingressPaths := .Values.ingress.paths -}}
{{- $ingressWebPaths := .Values.ingress.webPaths -}}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    app.kubernetes.io/name: {{ include "helpdesk.name" . }}
    helm.sh/chart: {{ include "helpdesk.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
{{- if .Values.ingress.tls }}
  tls:
  {{- range .Values.ingress.tls }}
    - hosts:
      {{- range .hosts }}
        - {{ . | quote }}
      {{- end }}
      secretName: {{ .secretName }}
  {{- end }}
{{- end }}
  rules:
  {{- range .Values.ingress.hosts }}
    - host: {{ . | quote }}
      http:
        paths:
  {{- range $ingressPaths }}
          - path: {{ . }}api/
            backend:
              serviceName: {{ $fullName }}
              servicePort: http
          - path: {{ . }}
            backend:
              serviceName: {{ $fullName }}-frontend
              servicePort: http
  {{- end }}
  {{- end }}
  {{- range .Values.ingress.webHosts }}
    - host: {{ . | quote }}
      http:
        paths:
  {{- range $ingressWebPaths }}
          - path: {{ . }}
            backend:
              serviceName: {{ $fullName }}
              servicePort: http
  {{- end }}
  {{- end }}
{{- end }}